# emacs_setup

## Install the Claude Code plugin

Run the following in Claude Code:

```
/plugin marketplace add hn12404988/emacs_setup
/plugin install mental-model-builder@willie-plugins
/reload-plugins
```
